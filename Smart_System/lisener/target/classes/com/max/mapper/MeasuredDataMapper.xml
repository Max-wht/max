<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.max.mapper.MeasuredDataMapper">

    <insert id="insertTest">
        insert into imitate (humidity,electricity,temperature,update_time)
        values(#{humidity},#{electricity},#{temperature},#{time})
    </insert>
</mapper>